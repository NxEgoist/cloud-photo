spring:
  application:
    name: cloud-photo-user
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: localhost:8848
      config:
        file-extension: yaml